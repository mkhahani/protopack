Bugs:
-----


TODO:
-----
- selectedRowIndex
- rowNumber
- cellSelect
- cellClasses
- mouseRollOver [row, cell]
- Support RTL
- Server side sorting
- Filters
- Keyboard Navigation (rowSelect, pageSelect, ...)
- There are some useful attrib for styling table (rule, frame, cellpadding, cellspacing)

Some advanced features:
-------------------------
- MultiSelect rows
- Row numbers
- Loading overlay
- To support more data structures (JSON, XML, PHP Array, ...)
- Skins
- Horizontal scrollbars
- Resizable Columns
- Selectable Columns
- Editing


- we should be able to set layout by some methods:
  setHeader("Sales,Book Title,Author")
  setInitWidths("50,250,*")
  setColAlign("right,left,left")
  setColTypes("dyn,ed,ed")
  setColSorting("str,str,str")
  enablePaging()
